# Real Staging Execution Gate V1

Status: **BLOCKED — NO REMOTE ACTION AUTHORIZED OR EXECUTED**

## Mandatory admission prerequisites

All must be present before a live driver is implemented or invoked:

1. The database lane is terminal and supplies its immutable branch/commit,
   safe Supabase project/region/PostgreSQL/PostGIS identity digests and accepted
   evidence paths.
2. The runtime lane is terminal and supplies its immutable branch/commit,
   image digest, deployment revision and HTTPS origin digest.
3. The user explicitly authorizes read-only staging verification and separately
   approves each state-changing failure drill, restore, restart or rollback
   action that is not inherently read-only.
4. An independent preflight observes every staging flag exact false and a
   zero-work disabled probe succeeds.
5. Proof access is supplied through ignored permission-restricted files or an
   approved secret store. Nothing is pasted into chat or placed on a command
   line. The verifier never prints, hashes or retains credential contents.
6. A non-secret approved production identity guard supplies only digests. No
   staging digest may match any production digest.
7. The observer trust key and owner decisions for receipt, backup/restore and
   alert-test age are approved independently.
8. No database import, migration, provider, V4 or other proof job shares the
   target during capture.

Missing immutable candidate identity, production ambiguity, credential need,
scope overlap, a provider call, or mutation risk is an immediate stop.

## Required live sequence

The future authorized driver must execute serially and re-attest bindings at
the end:

1. clean Git/tree/policy/historical-receipt attestation;
2. production guard and candidate cross-binding;
3. HTTPS/TLS, dependency-free liveness and safe readiness;
4. production preflight, image digest and non-root runtime;
5. database versions, all nine ordered roles, grants, RLS/Data API/function
   denials, and the exact cancellation/control privilege boundary;
6. migrations 001–008 and an independently timed second true no-op;
7. current active Harz/Innsbruck imports/projections, row totals, provenance,
   quarantine/residue and cross-region checks;
8. GiST/plan, reviewed latency distributions, cancellation/rollback/pool
   recovery, target-role restriction and business-data mutation denial;
9. isolated backup restore and all five reconciliations;
10. drain, restart, known-good rollback and roll-forward;
11. privacy-safe monitoring pipeline and delivery-tested alerts;
12. required and optional dependency outage/recovery drills;
13. provider-disabled endpoint with zero authorization, database, provider,
   budget, lease and circuit work;
14. provider ledger reconciliation before/during/after at exactly zero;
15. exact cleanup, final flags false, final candidate re-attestation, privacy
   scan, external observer signature and exclusive receipt publication.

Liveness must remain dependency-free. A required database outage must make
readiness `not_ready`; an optional monitoring outage may be `ready_degraded`
only under the approved dependency policy. Late work during drain, a rollback
to the same or unapproved revision, backup without a successful isolated
restore, stale/empty data, an unreviewed threshold, or any sensitive diagnostic
is NO-GO.

## Current blockers

- database lane terminal commit/resource identities: not supplied;
- runtime lane terminal commit/image/deployment/origin identities: not supplied;
- user read-only staging authorization: not supplied;
- failure-drill mutation authorization: not supplied;
- staging flags independently observed false: not supplied;
- proof access and observer trust material: not supplied;
- production digest guard: not supplied;
- operational age owner decisions: not supplied.

Therefore no remote staging, Supabase, HTTPS, backup, monitoring or deployment
operation has been attempted. Provider accounting is exactly zero and Attempt
13 remains unauthorized.
